{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": "> 0.25%, not dead",
        "modules": false // This ensures Babel does NOT convert import/export to require
      }
    ],
    "@babel/preset-typescript"
  ]
}
